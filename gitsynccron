*/5 * * * * echo "pulling" && cd /home/jovyan/programmieren-notebooks && git commit -a -m "saving local modifications" && git fetch origin && git merge -s ours origin/master -m "merging changes with strategy ours"
